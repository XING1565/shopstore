# .agents/issues

本目录用于存放各 Agent 在协作过程中产生、需要跨运行保留的 issue 辅助文件
（例如派生说明、工作镜像、中间笔记）。它不是应用代码，不参与构建。

| 用途 | Owner |
| --- | --- |
| issue 镜像 / 派生说明 | pm 规划、各 Agent 协作写入 |

正式的项目文档、issue 派发说明见 `docs/issues/` 与 `docs/`。
本目录内容仅作过程留痕，不作为交付物引用。
